# GasparGym — Cloudflare Deploy

Build command:
npm run build

Deploy command:
npx nitro deploy --prebuilt

The project uses Nitro's Cloudflare Module preset.
Do not set a custom Wrangler `main` pointing to `src/server.ts`.
